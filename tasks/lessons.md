
## 2026-04-24: Do not push without explicit final approval

When the user says they want to review first, or expresses uncertainty about a design, make local changes only and show the diff or preview. Do not commit or push unless the user explicitly says to commit or push after reviewing the final local result.

For website UI changes, prefer existing site components and rendering patterns before creating custom cards or new styling. If a bibliography item can be rendered through the existing bibliography template, use that instead of custom HTML/CSS.

- 2026-04-26: For this repository, do not commit or push changes unless the user explicitly asks for it.
